<main class="max-w-6xl mx-auto px-4">
<% if user_signed_in? %>
  Logged in as <%= current_user.email %> | <%= button_to 'Logout', destroy_user_session_path, method: :delete %>
<% end %>

  <div class="flex justify-center text-center">
  <h1 class="text-3xl font-bold underline">
  Hello world!
</h1>
  </div>
</main>